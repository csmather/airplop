# TODO

- [ ] Stable URL for phones — either a mDNS hostname (e.g. `airplop.local`) or a static LAN IP so the bookmark/tab survives reboots without re-scanning
- [ ] Auto-rerun port forwarding on boot so `setup_windows.ps1` doesn't need to be manually re-run after each reboot
- [ ] Send history — keep last N items, let the phone scroll back
- [ ] Simple PIN/password so other devices on the network can't snoop
- [ ] File/image support — drag-and-drop on the sender page, phone downloads it
